## Requirements

 - Python 3.6.5

## Setup

1.  Install opencv:

```
pip3 install opencv-python==4.1.0.25
```

2.  Place a video file in the same directory as `demo.py`

3.  Change the filename to match in `demo.py`

4.  Run: `python demo.py`
